= form_for @admin_panel_item_category do |f|
  - if @admin_panel_item_category.errors.any?
    #error_explanation
      %h2= "#{pluralize(@admin_panel_item_category.errors.count, "error")} prohibited this admin_panel_item_category from being saved:"
      %ul
        - @admin_panel_item_category.errors.full_messages.each do |message|
          %li= message

  .actions
    = f.submit 'Save'
