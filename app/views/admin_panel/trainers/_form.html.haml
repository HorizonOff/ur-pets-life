.row
  - if @trainer.errors.present?
    .form-group
      .col-md-12.col-sm-12.col-xs-12.error
        - @trainer.errors.full_messages.each do |m|
          %label= m
          %br
  = render partial: 'admin_panel/trainers/main_info_form', locals: { f: f }
  = render partial: 'admin_panel/qualifications/form', locals: { f: f }
  = render partial: 'admin_panel/trainers/location_form', locals: { f: f }
