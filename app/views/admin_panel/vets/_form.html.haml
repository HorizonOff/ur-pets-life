.row
  - if @vet.errors.present?
    .form-group
      .col-md-12.col-sm-12.col-xs-12.error
        - @vet.errors.full_messages.each do |m|
          %label= m
          %br
  = render partial: 'admin_panel/vets/main_info_form', locals: { f: f }
  = render partial: 'admin_panel/vqualifications/form', locals: { f: f }
  = render partial: 'admin_panel/vets/location_form', locals: { f: f }
