.row
  = render partial: 'layouts/admin_panel/errors', locals: { object: @vet }

  = render partial: 'admin_panel/vets/main_info_form', locals: { f: f }
  = render partial: 'admin_panel/qualifications/form', locals: { f: f }
  = render partial: 'admin_panel/vets/location_form', locals: { f: f }
