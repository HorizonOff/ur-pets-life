%div
  .page-title
    .title_left
      %h3 New boarding 

  .clearfix
  
  = form_for @boarding, url: admin_panel_boardings_path,
  html: { multipart: true, method: :post, class: 'form-horizontal form-label-left with_location', autocomplete: 'off'} do |f|

    = render partial: 'form', locals: { f: f }
    
    = f.submit 'Create boarding', class: 'btn btn-success pull-right'

:javascript
  init_map();
