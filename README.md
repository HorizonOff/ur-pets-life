#Project setup

  - Install ruby `rvm install "ruby-2.4.2"`
  - Create gemset `rvm gemset create ur_pets_life`
  - Install bundler `gem install bundler`
  - Install bundle for created gemset `bundle install`
  - Copy database configuration `cp config/database.example.yml config/database.yml`
